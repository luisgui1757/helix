// Prime dispatch — practical resource ceilings shared by every loop/cast path.
//
// JavaScript safe integers prevent arithmetic from stalling, while these lower
// ceilings keep structurally valid input operationally bounded. They are
// product rails, not spend controls.

/** Hard ceiling for every global, per-stage, debate, and research loop rail. */
export const MAX_ITERATIONS = 10_000;

/** Hard ceiling for any expanded candidate panel or concurrency request. */
export const MAX_PANEL_MEMBERS = 64;
