# Dependencies & build output
node_modules/
dist/
build/
coverage/

# Pi project-local package settings and caches are machine-owned. Helix is
# loaded from its package manifest (`pi -e .` while developing).
.pi/

# Local AI-tool settings (machine-specific)
.claude/

# Legacy in-repository state locations remain ignored for old checkouts. New
# installs write under Pi's user data directory instead.
dispatch/runs/
dispatch/local/

# Secrets / env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log
